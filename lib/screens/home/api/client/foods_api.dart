class FoodsApi {
  static const foods =
      '/foods?populate=*&pagination[page]=2&pagination[pageSize]=10';
  static const categories =
      '/foods?populate=*&pagination[page]=1&pagination[pageSize]=10';
}
